# 双相机同步采集系统硬件规格及接线说明

## 1. 文档目的

本文档说明双相机同步采集系统的硬件组成、相机性能、开发板条件、设备互联方式、供电要求、接线步骤及长期运行注意事项。

本文参数优先采用当前设备的 USB 描述符、V4L2 实测结果和项目实际配置。镜头焦距、传感器尺寸、光谱范围、测温范围、测温精度、防护等级等无法从当前设备或 SDK 中可靠读取的参数，应以相机铭牌和厂家正式数据表为准，不在本文中推测。

## 2. 系统组成

系统由以下硬件组成：

| 部件 | 当前设备/要求 | 数量 | 主要作用 |
|---|---|---:|---|
| 开发板 | TOPEET RK3588 LP4/LP5 Board | 1 | 运行 Linux、两个相机 SDK 和同步采集程序 |
| RGB 相机 | MindVision SUS507GC | 1 | 采集可见光彩色图像 |
| IR 相机 | USB ID `04b4:7510`，设备名 ThermaL Camera，项目 SDK 标识为 T1280 | 1 | 采集红外灰度图像，并通过虚拟串口执行控制命令 |
| USB 3.0 有源扩展坞 | SuperSpeed，带独立电源适配器 | 1 | 扩展开发板唯一可用的 USB 3.0 主机口并为两台相机供电 |
| USB 3.0 数据线 | 必须支持 SuperSpeed 5 Gbit/s | 3 根或按扩展坞结构配置 | 开发板至扩展坞、扩展坞至两台相机 |
| 开发板电源 | 使用开发板厂家规定的稳定电源 | 1 | 为 RK3588 开发板供电 |
| 扩展坞电源 | 使用扩展坞原装或满足其额定要求的电源 | 1 | 独立承担相机侧 USB 供电负载 |

## 3. RGB 相机规格

### 3.1 已确认参数

| 项目 | 规格 |
|---|---|
| 厂商 | MindVision |
| 型号 | SUS507GC |
| USB VID:PID | `f622:0001` |
| 设备协议 | USB3 Vision |
| USB 版本 | USB 3.0 |
| 要求链路速度 | SuperSpeed 5 Gbit/s |
| 传输类型 | USB Bulk |
| 当前输出图像 | 2448 × 2048，BGR8 彩色图像 |
| 当前有效像素数 | 5,013,504 像素，约 500 万像素 |
| 单帧应用层数据量 | 约 15.0 MB，按 2448 × 2048 × 3 字节计算 |
| SDK | MindVision Linux SDK，ARM64 动态库 `libMVSDK.so` |
| 工作方式 | 当前项目使用连续采集和回调取帧 |
| 白平衡 | 固定手动增益：R=1.37、G=1.00、B=2.07 |
| USB 供电声明 | 总线供电，USB 描述符声明最大 896 mA |
| 独立相机电源 | 相机能够仅通过 USB 供电；若仅使用USB供电，则长期系统中仍要求由有源扩展坞供电 |

### 3.2 性能说明

当前项目保存 RGB 图像的分辨率为 2448 × 2048，输出为 8 位三通道 BGR 数据。相机经 MindVision SDK 完成原始图像到 BGR8 的 ISP 转换。

相机 USB 描述符未直接给出当前项目使用的帧率，因此本文不将未经实测的帧率写成保证值。RGB 实际帧率受曝光时间、相机参数、USB 总线占用和 RK3588 图像处理负载影响，应以运行日志或厂家工具的实测值为准。

### 3.3 其余参数

以下参数请查阅 SUS507GC 对应镜头和传感器的正式资料：

- 传感器具体型号和尺寸
- 像元尺寸
- 镜头接口、焦距、光圈和视场角
- 动态范围、信噪比和曝光范围
- 硬件触发输入输出电气规格
- 工作温度、存储温度和防护等级

## 4. IR 相机规格

### 4.1 已确认参数

| 项目 | 规格 |
|---|---|
| USB 设备名 | General ThermaL Camera |
| 项目/SDK 标识 | T1280 / IRC USB SDK |
| USB VID:PID | `04b4:7510` |
| USB 版本 | USB 3.0 |
| 要求链路速度 | SuperSpeed 5 Gbit/s |
| 视频协议 | UVC 1.10 |
| 视频节点 | 当前实机为 `/dev/video21`，编号可能随启动和插拔变化 |
| 控制串口 | CDC ACM，当前实机为 `/dev/ttyACM0`，编号可能变化 |
| 串口参数 | 115200 bit/s，8N1，无硬件流控 |
| 视频分辨率 | 1280 × 1024 |
| 视频帧率 | 30 fps |
| USB 像素格式 | YUYV 4:2:2，16 bit/pixel |
| 单帧 USB 数据量 | 2,621,440 字节，约 2.50 MiB |
| 理论有效视频数据率 | 78,643,200 B/s，约 75 MiB/s 或 629 Mbit/s |
| 设备描述符最大码率 | 832 Mbit/s |
| 项目保存格式 | 提取 Y 分量，保存 1280 × 1024、8 位单通道灰度 PNG |
| USB 供电声明 | 总线供电，USB 描述符声明最大 400 mA |
| SDK | ARM64 `libIRCUSBSDK.so`，并依赖同目录的 libuvc、Poco 等库 |

### 4.2 复合 USB 设备说明

IR 相机只需要一根 USB 3.0 数据线。连接后，同一个物理设备会同时枚举为：

1. UVC 视频功能，形成 `/dev/video*`，传输 1280 × 1024 YUYV 视频流。
2. CDC ACM 控制功能，形成 `/dev/ttyACM*`，供 SDK 执行设备控制和快门校正。

`/dev/ttyACM0` 不是需要用户另接的板级 UART，也不能接到 RK3588 的 GPIO 串口引脚。它是 IR 相机通过同一根 USB 线虚拟出来的串口设备。SDK 会自动搜索并打开对应串口；运行脚本也会优先通过 `/dev/serial/by-id` 识别真实设备节点。

### 4.3 RK3588 USB 兼容性要求

当前 RK3588 Linux 5.10 内核环境中，该 IR 相机持续传输时必须为 USB 设备应用：

```text
04b4:7510:k
```

该参数用于禁用此设备的 USB 3.0 Link Power Management，避免长期 Bulk 视频传输发生 `EPROTO(-71)` 和 SDK `select timeout`。系统已通过 `thermal-camera-usb.service` 持久化该设置。

### 4.4 未确认参数

以下指标无法从当前 UVC 描述符和 SDK 可靠确认，必须以 IR 相机厂家数据表为准：

- 红外波段范围
- 探测器材料和像元尺寸
- NETD/热灵敏度
- 测温范围、测温精度和校准方式
- 镜头焦距、光圈和视场角
- 是否输出辐射测温原始值
- 工作温度、存储温度和防护等级

当前程序保存的是 YUYV 视频中的 8 位亮度灰度图，不应直接把像素值解释为绝对温度。若项目需要温度数据，应使用厂家提供的温度拉取、标定和换算接口，并依据厂家校准文件处理。

## 5. RK3588 开发板硬件条件

### 5.1 当前开发板配置

| 项目 | 当前实机配置 |
|---|---|
| 板卡 | TOPEET RK3588 LP4/LP5 Board |
| SoC | Rockchip RK3588 |
| CPU 架构 | ARM64 / aarch64，8 核异构 CPU |
| CPU 组成 | 4 × Cortex-A76 + 4 × Cortex-A55 |
| 当前内核识别频率 | A76 最高约 2.352 GHz，A55 最高约 1.8 GHz |
| 内存 | 约 8 GB，系统可见 7.7 GiB |
| 系统存储 | 约 32 GB eMMC，系统可见 29.1 GiB |
| 操作系统 | Ubuntu 22.04，Linux 5.10.226，aarch64 |
| 当前 USB 主链路 | xHCI USB 3.0，5 Gbit/s |
| GPU | RK3588 集成 GPU，当前频率表最高 1 GHz；本项目不依赖 GPU |
| NPU | RK3588 集成 NPU；本项目当前不依赖 NPU |

### 5.2 最低硬件条件

用于复现本项目的开发板或主机至少应满足：

- ARM64 环境下能够加载两个厂家提供的 aarch64 SDK；使用其他架构时必须有对应架构 SDK。
- 至少一个稳定的 USB 3.0 SuperSpeed 主机接口，实际拓扑必须显示 5000M。
- 建议不少于 4 GB 内存，当前部署为 8 GB。
- 存储空间应根据采集量规划。当前单组未压缩应用层图像约 16.3 MB，PNG 实际大小随场景变化。
- 能够同时处理约 75 MiB/s 的 IR 原始 USB 视频，以及 RGB 相机视频和 ISP 转换负载。
- 长期运行必须有可靠散热，RK3588 应安装散热器，建议使用风扇主动散热。
- 开发板电源必须使用厂家规定的额定输入，禁止使用 USB 扩展坞反向给开发板供电，除非扩展坞和开发板均明确支持合规 USB PD 输入。

## 6. 系统互联和同步原理

### 6.1 物理互联

两台相机之间没有直接连接线，也没有在当前系统中使用触发线、GPIO 同步线或板级 UART 线。数据拓扑为星形 USB 连接：

```text
开发板专用电源
      │
      ▼
TOPEET RK3588 开发板
      │
      │ 唯一 USB 3.0 Host，5 Gbit/s
      ▼
带独立电源的 USB 3.0 有源扩展坞 ◄──── 扩展坞独立电源适配器
      │
      ├──── USB 3.0 ──── MindVision SUS507GC RGB 相机
      │                  USB3 Vision + USB 供电
      │
      └──── USB 3.0 ──── ThermaL/T1280 IR 相机
                         UVC 视频 + CDC ACM 控制 + USB 供电
```

### 6.2 软件同步方式

当前实现属于主机软件时间戳同步，不是传感器级硬件同步：

1. 两台相机分别以连续模式采集。
2. RGB SDK 和 IR SDK 各自在收到帧时调用回调函数。
3. 程序使用 RK3588 的单调时钟 `steady_clock` 为帧记录主机接收时间戳。
4. 同步控制器比较两路队首时间戳。
5. 时间差小于 5 ms 时，保存为一组 VIS/IR 同步图像。
6. 时间差超过 5 ms 时，丢弃时间较早的一帧并继续匹配。

实机测试曾得到约 0.2983 ms 的单组匹配时间差，但这只是该次主机接收时间戳差，不代表两个传感器曝光起点的严格物理误差，也不能作为所有工况下的固定保证值。

### 6.3 同步能力边界

当前方案适用于普通双模态图像配对、静态场景和低速目标采集。如果需要高速运动目标、严格曝光同时或可量化的亚毫秒级物理同步，应改为硬件触发方案，并满足：

- 两台相机都提供兼容的触发输入或一台提供同步输出。
- 明确触发电平、电压、极性和脉宽，必要时加入隔离或电平转换。
- 使用同一硬件脉冲源触发两台相机。
- 结合曝光延迟、读出延迟和时间戳来源进行标定。

目前 IR 相机 SDK 的 `triggerOnce()` 未实现，现有接线也没有硬件触发线，因此不能把当前系统描述为硬件同步。

## 7. USB 带宽和供电预算

### 7.1 带宽要求

IR 相机的 UVC 描述符声明最大码率为 832 Mbit/s，已经明显高于 USB 2.0 的实际可用带宽，因此 IR 相机必须工作在 USB 3.0 5000M 模式。RGB 相机也要求 USB3 Vision SuperSpeed 连接。

两台相机通过同一个 USB 3.0 上行口共享 5 Gbit/s 标称带宽。必须选用真正支持 USB 3.x SuperSpeed 的扩展坞。

运行前应检查：

```bash
lsusb -t
```

RGB、IR 和扩展坞上行链路均应显示 `5000M`。IR 若显示 `480M`，不得开始采集。

### 7.2 供电预算

根据 USB 描述符，两个相机声明的最大总线电流为：

| 设备 | 最大声明电流 | 按 5 V 估算功率 |
|---|---:|---:|
| MindVision SUS507GC | 896 mA | 约 4.48 W |
| ThermaL Camera | 400 mA | 约 2.00 W |
| 合计 | 1296 mA | 约 6.48 W |

以上只是设备描述符声明值。扩展坞自身损耗、启动浪涌、线缆压降和设计余量尚未计入。长期使用建议扩展坞至少能够为下行设备稳定提供 5 V/3 A，总可用功率不低于 15 W；若还连接移动硬盘等设备，应进一步提高电源规格并按扩展坞厂家额定值选型。

## 8. 强制接线要求

### 8.1 推荐且唯一适合长期运行的接法

1. 关闭采集程序。
2. 给 RK3588 开发板连接其专用电源。
3. 给 USB 3.0 有源扩展坞连接独立电源，并先确认扩展坞供电稳定。
4. 使用 USB 3.0 SuperSpeed 上行线，将扩展坞连接到 RK3588 唯一的 USB 3.0 Host 接口。
5. 将 IR 相机连接到扩展坞的 USB 3.0 下行口。
6. 将 RGB 相机连接到扩展坞的另一个 USB 3.0 下行口，同时使用电源线给 RGB 相机供电。
7. 不要在 IR 相机与开发板之间另接 UART 线；`/dev/ttyACM0` 由 IR 相机 USB 线自动产生。
8. 启动开发板后执行 `lsusb -t`，确认 Hub 和两台相机均为 `5000M`。
9. 确认存在 IR 的 `/dev/video*` 和 `/dev/ttyACM*` 后再启动采集程序。

### 8.2 RGB 相机供电警告

SUS507GC 可以不连接独立相机电源，仅通过 USB 口供电。当前 USB 描述符声明其最大电流可达 896 mA。

禁止在长期运行场景中，让 RK3588 唯一 USB 3.0 Host 口直接承担 RGB 相机和 IR 相机的全部供电。这样会长期增加开发板 USB 5 V 供电路径、接口触点、保护器件和 PCB 走线的电流与热应力，可能造成：

- USB 端口过热和接触老化
- 电压跌落、相机重连或丢帧
- USB 控制器出现 `-71/EPROTO` 等链路错误
- 开发板 USB 供电开关、保险器件或接口损坏
- 缩短开发板 USB 3.0 端口及其供电电路的使用寿命


### 8.3 禁止接法

- 禁止使用无外接电源的被动 USB Hub 长期连接两台相机。
- 禁止将 IR 相机接到 USB 2.0 口或只支持 480M 的延长线。
- 禁止使用不确定是否支持数据的充电线。
- 禁止把 `/dev/ttyACM0` 当作物理 UART 再接到开发板 GPIO。
- 禁止热插拔松动、过长或屏蔽不良的 USB 线用于长期无人值守采集。
- 禁止把移动硬盘等高功耗设备加入同一无余量扩展坞电源预算。

## 9. 扩展坞和线缆选型要求

### 9.1 有源扩展坞

扩展坞应满足：

- USB 3.0/3.1 Gen 1 或更高，5 Gbit/s 以上。
- 独立电源输入，建议下行总供电能力不低于 5 V/3 A。
- 至少两个 USB 3.x 下行口。
- 明确支持多个高带宽 USB 设备同时工作。
- 具备过流、过压、短路和防倒灌保护。
- 不应依赖开发板上行口为扩展坞及两台相机供电。
- 工业长期运行优先选择金属外壳、锁紧接口或能够固定线缆的型号。

### 9.2 USB 线缆

线缆应满足：

- 明确标注 USB 3.x SuperSpeed 5 Gbit/s 或更高。
- 尽量短，建议每段不超过 1 m；必须加长时使用合格的主动延长方案。
- 具有完整屏蔽层和可靠接头。
- 避免与电机、电源逆变器和大电流线束并行敷设。
- 长期部署时对接头进行应力释放和机械固定，避免相机移动拉扯 USB 口。

## 10. 安装后硬件验收

### 10.1 USB 枚举

```bash
lsusb
```

应至少看到：

```text
f622:0001 MindVision SUS507GC
04b4:7510 ThermaL Camera
```

### 10.2 USB 速度

```bash
lsusb -t
```

应确认：

- 扩展坞上行链路：5000M
- RGB 相机：5000M
- IR 相机：5000M

### 10.3 IR 复合设备节点

```bash
ls -l /dev/video* /dev/ttyACM*
stty -F /dev/ttyACM0 -a
```

预期串口为 115200、8N1。设备编号可能不是固定的 `video21` 或 `ttyACM0`，程序应使用自动检测结果，不应在新系统中盲目硬编码编号。

### 10.4 USB 兼容参数

```bash
cat /sys/module/usbcore/parameters/quirks
systemctl status thermal-camera-usb.service
```

预期包含：

```text
04b4:7510:k
```

服务应为 `active`，并设置为 `enabled`。

### 10.5 采集验证

```bash
cd /home/topeet/Camera_project/camera_project
./run.sh
```

一次采集应生成：

```text
sync_pair_<编号>_<模式>_vis.png
sync_pair_<编号>_<模式>_ir.png
```

同时检查：

```bash
dmesg | grep -E 'uvcvideo|EPROTO|-71|disconnect|reset'
```

长期运行中不应反复出现 `-71`、USB reset、disconnect 或 `select timeout`。

## 11. 长期运行建议

- 必须使用有独立电源的 USB 3.0 扩展坞。
- 开发板和扩展坞应使用稳定、留有余量的电源适配器。
- 给 RK3588 配置主动散热，并监控 CPU 温度和降频情况。
- 固定相机、扩展坞和线缆，避免 USB 插头受力。
- 定期检查扩展坞电源、USB 接口和线材是否异常发热。
- 数据长期保存时使用独立存储介质，并把其功耗计入扩展坞或独立供电预算。
- 正式部署前至少进行 24 小时连续采集测试，记录丢帧、USB 重连、温度和存储写入状态。
- 相机视场对齐属于机械安装和标定问题；若需要 RGB/IR 像素级融合，应另外完成内参、畸变、外参和跨模态配准标定。

## 12. 结论

本系统使用 RK3588 作为双相机主机，通过一个带独立电源的 USB 3.0 扩展坞连接 MindVision SUS507GC RGB 相机和 1280 × 1024@30 fps IR 相机。IR 的视频和 115200 波特率控制串口由同一根 USB 线提供，不需要额外串口接线。

由于开发板只有一个可用 USB 3.0 Host 接口，两台高带宽相机必须共享该链路。长期可靠运行的关键条件是：使用真正的 SuperSpeed 有源扩展坞、由扩展坞独立承担相机供电、两台相机均保持 5000M 连接，并为 IR 相机应用 `04b4:7510:k` USB 兼容参数。当前同步方式为 5 ms 阈值的主机软件时间戳配对，不等同于硬件曝光同步。
